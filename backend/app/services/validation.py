"""
Validation service — Phase 1 placeholder.

Will verify:
  1. For each question: sum(marks_entries) == question_totals value.
  2. Grand total: sum(question_totals) == total_marks_declared.
Mismatches are flagged, not silently corrected.
"""
# Phase 1 implementation goes here.
